apiVersion: v1
kind: Secret
metadata:
  name: aws-route53-credentials
  namespace: cert-manager
type: Opaque
stringData:
  access-key-id: "{{ _aws_access_key_id }}"
  secret-access-key: "{{ _aws_secret_access_key }}"
